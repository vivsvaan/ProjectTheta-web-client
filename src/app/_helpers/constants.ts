/**
 * Constants being used in whole app
 */

import { Theta } from '../_vendors/vendor.struct';

export const SERVER_URL = 'http://127.0.0.1:8000/';

export const APP_SECRET = 'DEV_TEST_API';

export const enum LOCAL_STORAGE_KEYS {
    UserInfo = 'userInfo',
};

export const SELECTED_VENDOR = Theta